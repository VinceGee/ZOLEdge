<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="zw.co.vokers.zoledge">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" />

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <!--camera permission for scan Qr code-->
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".ZOLEdgeApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Info">
        <activity
            android:name=".activities.SplashScreen"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.ProductDetails"
            android:theme="@style/Info" />

        <activity
            android:name=".activities.BaseStationDetails"
            android:theme="@style/Info" />
        <activity
            android:name=".activities.Main2Activity"
            android:theme="@style/Info"/>

        <activity android:name=".activities.TroubleFibroniks"
            android:label="Troubleshooting GPON"
            android:theme="@style/ForDrawer" />

        <activity android:name=".activities.MissionVisionActivity"
            android:label="ZOL Zimbabwe"
            android:theme="@style/Mission" />

        <activity android:name=".activities.TroubleVsat"
            android:label="Troubleshooting VSAT"
            android:theme="@style/ForDrawer" />

        <activity android:name=".activities.EmailsTrouble"
            android:label="Troubleshooting Emails"
            android:theme="@style/ForDrawer" />

        <activity android:name=".activities.TroubleWimax"
            android:label="Troubleshooting Wimax"
            android:theme="@style/ForDrawer" />

        <activity android:name=".activities.TroubleDNSActivity"
            android:label="DNS"
            android:theme="@style/ForDrawer" />

        <activity android:name=".activities.PaymentMethods"
            android:label="ZOL Payment Methods"
            android:theme="@style/ForDrawer" />

        <activity android:name=".activities.TroubleMpls"
            android:label="MPLS Connections"
            android:theme="@style/ForDrawer" />

        <activity android:name=".activities.ZOLFupActivity"
            android:label="ZOL Fair Usage Policy"
            android:theme="@style/ForDrawer" />

        <activity android:name=".activities.WifiScanner"
            android:label="ZOL WiFi"
            android:theme="@style/ForDrawer" />

        <activity android:name=".activities.QRGenerateActivity"
            android:label="ZOL Fair Usage Policy"
            android:theme="@style/ForDrawer" />

        <activity android:name=".activities.QROptionActivity"
            android:label="ZOL WiFi"
            android:theme="@style/ForDrawer" />

        <activity android:name=".utils.BarcodeCaptureActivity"
            android:label="ZOL WiFi"
            android:theme="@style/ForDrawer" />

        <activity android:name=".activities.WiFiConnectActivity"
            android:label="ZOL WiFi"
            android:theme="@style/ForDrawer" />

        <activity android:name=".activities.QuizActivity"
            android:label="ZOL CSR Personal Quiz"
            android:configChanges="orientation"
            android:screenOrientation="portrait"
            android:theme="@style/ForDrawer" />

        <activity android:name=".activities.CallCentreManual"
            android:label="ZOL Call Centre Manual"
            android:theme="@style/ForDrawer" />

        <activity android:name=".activities.FrontDeskSupportActivity"
            android:label="ZOL Front Desk Support"
            android:theme="@style/ForDrawer" />

        <activity android:name=".activities.PilotsAlphabet"
            android:label="The Pilot's Alphabet"
            android:theme="@style/ForDrawer" />

        <activity android:name=".activities.ZolDirectory"
            android:label="ZOL Directory"
            android:theme="@style/ForDrawer" />

        <activity
            android:name=".activities.ScanActivity"
            android:screenOrientation="portrait"
            />

        <activity
            android:name=".activities.TicketResultActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar"/>
    </application>

</manifest>